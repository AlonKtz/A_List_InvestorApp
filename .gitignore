# config.js is committed intentionally — for a client-side Appwrite app these
# values (endpoint, project/db/collection IDs) are public by design. Security
# is enforced by the Appwrite platform allowlist + document permissions.

# Personal presentation material — not part of the deliverable
project speech - hebrew.docx

# Design-system handoff reference (React kit) — source material, not part of the app
The A List — Design System-handoff/

# Handoff doc written by me for the college system
By Alon Katz.docx